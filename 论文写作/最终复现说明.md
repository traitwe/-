# 最终复现说明

在项目根目录运行：

```powershell
python scripts/run_paper_mainline.py
python -m pytest -q
```

主线脚本会重建问题一至问题四的正式输出；相对资源档位、Pareto 作图和 Q2 消融脚本不属于正文主线。随后使用 XeLaTeX 编译 `论文相关模板/国赛Latex模板/国赛模板/B题完整论文_国赛模板.tex`。

封稿前只允许更新由上述主线重新生成的 `outputs`，并需重新核对正文表 2--表 5 与对应 CSV。停车与接驳锚点的边界见 `停车与接驳真实锚点接入说明.md`。
