# 问题一模型说明与封装

问题一主链采用分层左删失似然：上榜景点贡献对数正态密度，未上榜景点贡献低于同片区同采样日阈值的累计概率。解析梯度 L-BFGS-B 已在真实数据上收敛。

主输出：`data/model_input/daily_region_visitor_scale_censored_likelihood_2023_2025.csv`。其中保守、基准、高值均为锚点约束估计，不是官方逐日真实人次。

质量报告：`data/model_input/censored_likelihood_quality_report.json`；模型与岭回归对比：`data/model_input/censored_likelihood_vs_ridge_validation.csv`。主模型在 510 个有效采样组上收敛，目标函数为 -748.0082。

重建顺序：

```powershell
$py='C:\Users\lyy20\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe'
& $py scripts\build_hierarchical_censored_pressure.py
& $py scripts\build_censored_likelihood_visitor_scale.py
```

`daily_region_censored_likelihood_pressure_2023_2025.csv` 是相对压力主输出；`visitor_scale_censored_likelihood_anchor_fit.csv` 审计尺度锚点。CITY、ARA、BND 和子景区锚点未直接并入三片区估计；短期冲突锚点保留为诊断项。
