# 问题一补充交付：全市周期与日内时段

## 全市日级周期序列

输出 `outputs/question1_analysis/q1_city_daily_annual_constrained.csv` 覆盖 2023、2024 两年，共 731 天。令三区锚点约束下的日级游客规模之和为 \(S_t\)，当年官方全市国内旅游人次为 \(Y_y\)，则：

\[
\hat Y_t^{city}=Y_y\frac{S_t}{\sum_{\tau\in y}S_\tau}.
\]

因此每年的日级估计和严格等于对应官方年度总量。它的日级形态来自三区核心片区，而年度尺度来自全市官方统计；应称为“年度总量约束下的全市日级估计”，不能称为官方逐日实测值。2025 年没有年度官方锚点，未进入该序列。

配套结果：

- `q1_city_monthly_cyclic_profile.csv`：逐年逐月估计总量及其年度占比；
- `q1_city_daily_decomposition.csv`：全市估计日级序列的趋势、月度季节项与随机项。

## 单日进、离场时段

输出 `outputs/question1_analysis/q1_city_entry_exit_time_profiles.csv` 是原建模方案 \(\omega_{r,\tau}\) 的可执行形式。根据已有的日出、日间服务、夜游服务窗口，按 30 分钟离散为进场与离场权重；每个情景的进场权重和、离场权重和均为 1。

日间服务默认进场峰位于服务窗的 25%，离场峰位于 78%；日出情景进场峰前移至 18%，夜游情景离场峰后移至 88%。峰位允许在服务窗内前后移动 10% 进行敏感性分析。

所有记录均为 `assumption_driven_not_observed_hourly_flow`：它可用于将日级需求拆分到服务时段，不能表述为实测小时进场量、离场量或进入/离开比例。
