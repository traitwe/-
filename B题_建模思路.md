# B题多尺度旅游压力生成、预测与资源配置模型设计

## 1. 目标与原则

为“秦皇岛市域游客多尺度流量预测及景区服务资源动态配置优化”建立一条连续的建模主线：

```text
多源异构观测 → 潜在旅游压力生成 → 多尺度预测 → 资源配置优化 → 情景鲁棒检验
```

模型名称：**融合连续热度状态、分层观测融合与弱空间约束的多尺度旅游压力生成模型**。

建模遵守以下口径原则：

- 全市年度游客量为官方绝对量锚点；
- 景区客流指数、拥堵指数为相对观测，不能直接称作游客人数；
- 全市月度结果是年度约束下的季节分配估计，不是实测月度客流；
- 三片区日级结果是旅游压力指数及其预测区间，不伪装成真实日游客量；
- 停车、酒店、交通 POI 用于空间供给与优先级刻画，不能直接视为实际容量；
- 缺失值不补零；后验不确定性必须随观测稀缺程度增大。

## 2. 数据可识别性审查

| 数据类别 | 当前覆盖 | 可支撑的用途 | 不可支撑的结论 |
|---|---|---|---|
| 全市年度国内游客量 | 2002—2024 年 | 年度绝对量趋势与预测 | 直接验证月度/日级真实客流 |
| 百度热度 | 2016-01-01—2026-08-08 连续日级、4 个关键词 | 连续状态识别、季节性和短期先行特征 | 单独等同于客流 |
| 天气与日历 | 2015—2025 天气、2015—2026 日历连续 | 外生驱动、未来情景 | 精确解释所有突发事件 |
| 景区客流/拥堵指数 | 896 行、371 个日期、18 个景区，日期不连续 | 片区压力的零散观测校正 | 连续日客流或绝对游客人数 |
| 三片区共同观测 | 北戴河、海港、山海关同时观测 27 天 | 弱空间先验、同步性描述 | 高自由度空间传导系数 |
| 报道型客流锚点 | 2023—2025 节假日、暑期等点/区间 | 局部外部校准 | 连续暑期总客流序列 |
| 资源锚点与 POI | 停车、酒店、公交 POI 与少量报道参数 | 资源配置档位和空间优先级 | 精确车位数、人员数、班次台账 |

三大输出片区统一记为：北戴河、山海关、海港—阿那亚综合旅游压力片区。第三片区中，海港景区指数是日级观测，阿那亚热度与报道锚点是补充信息；不能声称已获得阿那亚单独真实日客流。

## 3. 变量与层级

令区域集合为：

\[
r\in\{\mathrm{BDH},\mathrm{SHG},\mathrm{HGA\text{-}ARY}\}.
\]

令 \(F_{r,t}>0\) 为区域 \(r\) 在日期 \(t\) 的潜在旅游压力；\(G_t\) 为全市共同旅游状态；\(S_t\) 为日级隐状态。

\[
S_t\in\{\text{常态},\text{高压}\}.
\]

外生变量：

\[
\mathbf Z_t=(\text{暑期},\text{周末},\text{法定节假日},\text{已知活动}),
\qquad
\mathbf W_t=(\text{降雨},\text{温度},\text{天气舒适度}),
\qquad
\mathbf H_t=(\text{搜索主题热度因子}).
\]

## 4. 问题一：旅游压力生成与机制识别

### 4.1 连续状态层

由可提前获知的日历和活动变量建立两状态非齐次马尔可夫链：

\[
P(S_t=j\mid S_{t-1}=i,\mathbf Z_t)
=
\frac{\exp(\gamma_{ij}^{\top}\mathbf Z_t)}
{\sum_k\exp(\gamma_{ik}^{\top}\mathbf Z_t)}.
\]

状态含义：常态为普通日；高压为暑期周末、法定节假日或已知活动峰值。天气不再另设“扰动状态”，而是在压力方程中作为直接冲击，避免用同一因素重复解释状态和压力强度。

### 4.2 全市共同压力动态层

\[
G_t
=
\mu_{S_t}
+\phi G_{t-1}
+\boldsymbol\beta^{\top}\mathbf H_{t-1}
+\boldsymbol\gamma^{\top}\mathbf W_t
+\varepsilon_t.
\]

\(G_t\) 是唯一的动态核心：日历只改变“常态/高压”机制，搜索热度解释压力强度，天气构成直接冲击。预测时不得使用当天或未来搜索热度；7 日内采用 \(t-1,t-7\) 滞后因子，30 日以上先预测热度因子或改用高/中/低热度情景。片区压力不再另设一套同类动态方程，而由 4.4 节从 \(G_t\) 分解得到。

### 4.3 景区观测融合层

景区 \(i\) 的客流或拥堵指标记为 \(Z_{i,t}\)：

\[
\log(1+Z_{i,t})
\sim
\mathcal N(a_i+\lambda_iF_{r(i),t},\sigma_i^2).
\]

在同片区同日有多个景区观测时，先形成稳健区域观测：

\[
R_{r,t}
=
\operatorname{Median}_{i\in r}
\left[w_1\widetilde V_{i,t}+w_2\widetilde C_{i,t}\right],
\]

并按可观测景区数量和离散度设置观测误差：

\[
R_{r,t}\sim\mathcal N(F_{r,t},\sigma_{r,t}^2),
\qquad
\sigma_{r,t}^2\propto\frac{\text{观测离散度}}{n_{r,t}}.
\]

这保留动态因子的“共同压力”思想，但避免在观测极不均衡时估计过多景区参数。

对 A3b 的未出现记录不能按随机缺失处理：数据仅保留当日进入全国拥挤排名前列的景点。令 \(O_{i,t}\) 为是否被收录，则加入选择性观测方程

\[
O_{i,t}=\mathbb 1\{a_i+\lambda_iF_{r(i),t}+\epsilon_{i,t}>c_t\}.
\]

\(c_t\) 为当日或月份的入选阈值；已观测记录贡献正态密度，未观测记录贡献低于阈值的累积分布 \(\Phi\)。因此，淡季未上榜是“压力偏低”的负向信息，而不是真实零客流或无信息缺失；该删失似然避免因选择性观测压缩季节振幅。

### 4.4 共同压力与片区异质性层

因四片区同步观测极少，不估计空间传播、空间 VAR 或图拉普拉斯平滑参数。采用全市共同因子、片区长期异质性和当天独立偏离：

\[
F_{r,t}=G_t+b_r+u_{r,t},
\qquad
u_{r,t}=\rho_r u_{r,t-1}+\xi_{r,t}.
\]

其中 \(b_r\) 表示片区稳定差异，由海滨/历史文化属性、POI 结构、可达性和有效可游览面积作解释性描述；\(v_{r,t}\) 表示同受共同日历、热度和天气驱动后仍存在的片区当日偏离。仅在共同观测日期上报告片区相关系数作为描述性验证，不把它解释为游客跨片区传播或精确分流比例。

### 4.5 年度与报道锚点的软校准

年度官方游客量：

\[
Y_y^{\text{official}}
\sim
\mathcal N\left(\kappa_y\sum_{t\in y}G_t,\sigma_y^2\right).
\]

为保证尺度可识别，固定基准年 \(\kappa_{2019}=1\)，其余年份的 \(\log\kappa_y\) 服从小方差随机游走先验；它仅吸收统计口径、景区覆盖率和旅游结构的缓慢变化，不能与 \(G_t\) 任意相互抵消。

节假日和暑期报道作为局部软锚点：

\[
A_k
\sim
\mathcal N\left(\kappa_k\sum_{t\in\mathcal T_k}F_{r_k,t},\sigma_k^2\right).
\]

这些锚点不相互累加，只用于约束对应的时间区间和空间口径。

### 4.6 问题一输出

- 三片区日级旅游压力后验均值和 90% 可信区间；
- 全市共同旅游状态及每日状态概率；
- 节假日、暑期、热度、天气、活动在不同状态下的影响；
- 区域共同波动和弱空间联动；
- 缺失观测对不确定性的影响；
- 旺季、淡季和高压日的机制说明。

### 4.7 季节性分解与在园量的辅助校验

对 17 个关键词先按“城市目的地、海滨度假、景点文化、出行服务”分组，在组内标准化并提取 2--4 个可解释主题因子；不将 17 个原始词同时入模。再计算年内、月内、周内的熵冗余/峰谷比，用于识别暑期、周末和法定假日的集中性；熵指标只解释季节结构和辅助设定高压阈值，不进入预测方程，也不将搜索热度直接解释为游客人数。

#### 4.7.1 搜索关键词质量分级与语义约束

在主题分组前，先按零值率、缺失形态、结构断点和旅游语义进行质量筛选，而不是因关键词热度高便默认其可代表旅游需求：

| 等级 | 判据 | 建模处理 |
|---|---|---|
| A | 零值率低、无显著结构断点，且语义直接指向目的地、片区或景点 | 进入旅游需求主题因子的主样本 |
| B | 零值/缺失中等但无长期断裂 | 仅对短缺口作时间序列插补，同时保留缺失指示并在因子构造中降权 |
| C | 大量零值、显示阈值删失或疫情期零值高度集中 | 不进入主需求因子；必要时仅作购票/攻略等辅助意图信号 |
| D | 存在明显口径变更或长期结构断点 | 不跨断点插补、比较或训练；仅使用断点后同口径样本，或直接剔除 |

“秦皇岛天气”等天气查询词即使零值率很低、指数很高，也仅作为天气控制信息，不并入旅游需求主题因子；否则会因其量级优势主导因子。2020--2022 年的零值集中同时可能反映疫情冲击和平台显示阈值，不能直接解释为真实零搜索需求。最终主因子应主要由 A 类目的地/片区/景点词构成，而非机械只保留最初四个关键词。所有进入模型的搜索主题因子均按年标准化（或等价地加入年度固定效应），以消除平台份额漂移；周内形状只由日历变量承担，热度因子不用于解释周末效应。各片区允许使用不同的滞后热度特征集合。

对需要进入承载约束的“在园压力”，采用停留时间分布的卷积表达，而不独立虚构离园流：

\[
N_{r}(t)=\int_0^\infty I_r(t-u)\Pr(H_r>u)\,\mathrm du.
\]

其中 \(I_r^{(s)}=\kappa_r^{(s)}F_{r,t}\) 为由预测压力转换得到的情景到达需求，\(H_r\) 为按片区和场景设定的停留时间。由于没有本地连续入园、离园记录，此式只用于比较不同停留情景下的相对在园压力与容量是否被触发，不输出“真实在园人数”，也不回流到问题一、二的客流拟合。

转换系数不设为全市统一、可被解释为真实客流的单一参数，而按片区构造保守、基准和高压情景：

\[
I_{r,t}^{(s)}=\kappa_r^{(s)}F_{r,t},
\qquad s\in\{\text{保守},\text{基准},\text{高压}\}.
\]

对存在节假日、暑期或景区累计接待量锚点的片区，以对应时期 \(\mathcal T_k\) 校准候选系数：

\[
\kappa_{r,k}=\frac{A_{r,k}}{\sum_{t\in\mathcal T_k}F_{r,t}}.
\]

同片区多个候选值的中位数为基准情景，低/高分位数为保守/高压情景；不能把全市年度游客量按固定比例硬分给片区。阿那亚缺少连续独立客流时，\(\kappa_{\mathrm{ARA}}^{(s)}\) 仅由预约上限、有效面积和报道锚点限定，用于容量触发情景，不报告为阿那亚真实日客流估计。

### 4.8 双层输出与绝对游客规模软校准

为同时完成“连续客流时序构建”和“片区短期逐日游客量预测”，模型输出分为两层：

1. \(\hat F_{r,t}\)：旅游压力指数，负责描述连续日形态、状态切换和不确定性；
2. \(\hat V_{r,t}\)：锚点约束下的片区日游客规模估计，负责回答题目中的逐日游客量需求。

绝对规模不由单一固定比例直接放大，而由片区--年度标度和状态修正共同校准：

\[
\hat V_{r,t}
=
\hat F_{r,t}\,\theta_{r,y}\,q_{S_t},
\]

其中 \(\theta_{r,y}\) 由年度官方游客量、区域暑期/节假日累计锚点和日级报道锚点联合软校准，\(q_{S_t}\) 只区分常态与高压两种标度。校准满足：

\[
\sum_r\sum_{t\in y}\hat V_{r,t}
\approx \chi_yY_y^{\mathrm{official}},
\qquad 0<\chi_y\le 1,
\qquad
\sum_{t\in\mathcal T_k}\hat V_{r_k,t}
\approx A_k.
\]

\(\chi_y\) 表示三大片区覆盖全市游客的比例，只能作为区间/情景参数，绝不默认等于 1。区域暑期、节假日和日级锚点优先校准对应片区；全市年度总量仅约束共同趋势与三大片区合计不超过全市。所有 \(\hat V_{r,t}\) 均标为“锚点约束下的估计值”，同时报告由 \(\theta_{r,y}\)、\(\chi_y\)、状态修正和锚点稀疏性传播得到的区间；不得称为连续真实客流。若某年度缺少官方总量，绝对规模只按情景预测输出，并与相对压力指数分开呈现。

## 5. 问题二：多尺度预测与评价

未来预测由问题一模型递推：

\[
p(\mathbf F_{t+h},S_{t+h}\mid\mathcal D).
\]

### 5.1 全市年度和月度结果

年度绝对量预测：

\[
\hat Y_{y+1}.
\]

月度规模通过日级共同状态按月聚合：

\[
\hat Y_{y,m}
=
\hat Y_y
\cdot
\frac{\sum_{t\in(y,m)}\exp(\hat G_t)}
{\sum_{t\in y}\exp(\hat G_t)}.
\]

因此：

\[
\sum_{m=1}^{12}\hat Y_{y,m}=\hat Y_y.
\]

月度结果须称为“年度约束下的月度游客规模估计”。

### 5.2 区域日级结果

输出以下分位数：

\[
\hat F_{r,t}^{0.1},\quad
\hat F_{r,t}^{0.5},\quad
\hat F_{r,t}^{0.9}.
\]

预测窗口分为：1—7 天条件预测、8—30 天情景预测、暑期和法定假日峰值区间预测。

### 5.3 验证策略

- 年度游客量采用滚动起点验证；
- 区域压力仅在存在景区指数观测的日期进行时间滚动验证；
- 月度结果用节假日和暑期官方报道作外部合理性校验；
- 与季节朴素法、动态回归/SARIMAX 基准比较 MAE、RMSE、SMAPE；
- 区间预测报告覆盖率 PICP 和平均区间宽度。

## 6. 问题三（旧版，已废止）：基于压力分位数的资源配置强度优化

> 以下旧版将三区总游客规模直接映射到单条接驳线路或局部停车锚点，存在服务范围与数量级不匹配问题，仅保留其“单位不得混合”的原则，不再作为论文方案或代码实现依据。

现有数据不足以支持精确人员数、真实车位数和完整班次台账，因此决策变量定义为相对配置强度或档位：

\[
x_{r,t}^{p},x_{r,t}^{b},x_{r,t}^{k},x_{r,t}^{w}.
\]

分别对应人员、接驳、停车和窗口的常态、加密、应急等配置档位。

后验抽样产生需求情景：

\[
D_{r,t}^{(s)}=\kappa F_{r,t}^{(s)}.
\]

成本目标：

\[
\min C=
\sum_{r,t}(c_px_{r,t}^p+c_bx_{r,t}^b+c_kx_{r,t}^k+c_wx_{r,t}^w).
\]

服务损失目标：

\[
\min L=
\sum_{r,t,s}\pi_s
\left[a(D_{r,t}^{(s)}-Q_{r,t})_+
+b\cdot\text{停车紧张}
+c\cdot\text{接驳等待}\right].
\]

其中服务能力 \(Q_{r,t}\) 由各类资源配置强度组成。真实资源锚点给出上下界，POI 决定空间优先级；最终输出淡季、平季和暑期高峰的资源档位方案，而不是虚构精确人数。

### 6.1 文献驱动的可实施升级

资源配置不再只比较“资源成本”与“未服务压力”，而是把预测结果转译为时变、可执行的服务能力约束。该升级吸收停车动态分配、时变排队服务、分时入园和接驳频率优化文献的共同思想，但只保留当前数据能够支持的部分。

首先，将日级需求样本按日内服务窗口拆分为早、中、晚时段。令 \(\omega_{r,\tau}\) 为片区 \(r\) 在服务时段 \(\tau\) 的需求占比：没有小时级计数时，它不是待估的真实到达率，而是由开放时间、暑期/节假日情景和敏感性分析给出的情景参数，满足 \(\sum_\tau\omega_{r,\tau}=1\)。于是：

\[
D_{r,t,\tau}^{(s)}=\omega_{r,\tau}D_{r,t}^{(s)}.
\]

问题三不再以一个泛化的 \(D,Q\) 混合不同物理单位。以日游客规模估计 \(\hat V_{r,t}\) 为共同来源，定义入园需求 \(D^{\mathrm{visit}}_{r,t,\tau}=\omega_{r,\tau}\hat V_{r,t}\)（人次/时段），再分别转换为：

\[
D^{\mathrm{park}}_{r,t,\tau}=\frac{p_r^{\mathrm{car}}D^{\mathrm{visit}}_{r,t,\tau}}{m_r^{\mathrm{car}}}\quad(\text{车辆/时段}),
\qquad
D^{\mathrm{bus}}_{r,t,\tau}=p_r^{\mathrm{bus}}D^{\mathrm{visit}}_{r,t,\tau}\quad(\text{乘车人次/时段}).
\]

停车能力 \(Q^{\mathrm{park}}\) 以车辆/时段表示（车位数乘该时段周转率）；接驳能力 \(Q^{\mathrm{bus}}\) 以乘车人次/时段表示；入口能力 \(Q^{\mathrm{entry}}\) 以入园人次/时段表示；承载能力 \(Q^{\mathrm{onsite}}\) 以可同时在园人数表示。每个缺口只在同单位内计算，禁止把车位、车辆、游客人次和在园人数直接相加。

其次，以“服务水平代理”替代无法校准的精确排队分布。对窗口、接驳和停车分别构造供需缺口：

\[
g_{r,t,\tau}^{j,(s)}=(D_{r,t,\tau}^{j,(s)}-Q_{r,t,\tau}^{j})_+,
\qquad j\in\{w,b,p\}.
\]

其中 \(Q^w,Q^b,Q^p\) 分别为窗口、接驳、停车的情景服务能力。优化不声称得到真实平均等待时间，而以缺口 \(g\) 控制“等待超目标风险”的代理量；这继承时变排队理论中“按时段配置以稳定服务水平”的思想，同时避免缺少到达过程、服务时长和实际排队记录时的参数幻觉。

停车部分采用“片区停车池”而非逐车位实时调度。可用车位锚点、停车 POI 和外圈换乘点共同决定 \(Q^p\) 的下限、基准和扩容情景；当核心停车缺口为正时，允许向外圈共享池转移需求，并计入额外步行/换乘不便：

\[
g_{r,t,\tau}^{p,(s)}\le q_{r,t,\tau}^{\mathrm{core}}+q_{r,t,\tau}^{\mathrm{transfer}},
\qquad
q_{r,t,\tau}^{\mathrm{transfer}}\le \overline Q_r^{\mathrm{transfer}}.
\]

这保留多停车场共享分配的核心优点——缓解核心区饱和、平衡利用率——但不虚构逐停车场实时空位、车辆路径或动态定价数据。

接驳部分将发车间隔 \(h_{r,t,\tau}\) 作为档位决策，并由公开服务时段、最低已知间隔和车辆载客率情景形成能力：

\[
Q_{r,t,\tau}^{b}
=
\frac{T_{r,\tau}}{h_{r,t,\tau}}\,c_b\,\eta_b,
\]

其中 \(T_{r,\tau}\) 是可运营时长，\(c_b\) 为标准/情景单车运力，\(\eta_b\) 为满载率情景。目标函数中加入随候车压力非线性增大的惩罚 \(\Psi_b(g^b)\)，而非把等待不便简化为线性常数；这对应接驳频率优化中“等待时间越长，满意度下降越快”的结论。

承载力约束由单一面积除法扩展为“空间—预约—拥挤”三重上限。面积必须先经“是否开放、是否可游览、是否仅为通道/办公/后勤”筛选；\(S_0\) 为地理边界、\(S_1\) 为景区占地，二者均不得直接进入承载公式。有效可游览面积定义为 \(X_r=\kappa_A S_{1,r}\)，其中 \(\kappa_A\) 采用已有 0.20--0.40 情景范围。

\[
Q_{r,t,\tau}^{\mathrm{visit}}
\le
\min\left\{
\frac{A_r^{(a)}}{a_r^{(s)}},\;
\frac{B_{r,t,\tau}^{\mathrm{reservation}}}{\rho_r^{(s)}},\;
\overline Q_{r,t,\tau}^{\mathrm{safe}}
\right\}.
\]

\(A_r^{(a)}\) 指上述 \(X_r\) 的低/基准/高情景，\(a_r^{(s)}\) 来自与空间类型匹配的人均标准，\(B^{\mathrm{reservation}}\) 仅在存在预约约束时启用，\(\rho\) 为预约到访率情景。日承载能力还须乘以分空间周转率 \(Z_i\)：\(C_{\mathrm{day}}=\sum_i(X_i/a_i)Z_i\)。滨海沙滩/水域使用 QHD-07 的专门标准，且 \(Z=1\)，不得套用一般主题公园标准或统一停留时间周转率。阿那亚采用预约上限优先的硬约束；其余片区若无本地预约数据，则不将全国预约率伪装为本地实测，仅作为敏感性情景。另以 \(\Psi_c(\cdot)\) 惩罚接近承载上限时的拥挤体验损失，避免“未超过静态容量即无损失”的不合理假设。

滨海空间单独按官方规划的沙滩/水域标准取值，不与一般公园、古街或建筑空间共用人均面积；沙滩采用保守端，水域采用中位端。滨海游览的日周转率设为 1，避免以统一停留时长把海滩的日承载量虚增为 2--3 倍。规划床位、旅游从业人员比等只作为供给规模和量级合理性校验，不能替代逐家酒店入住率或景区运营人员实测。

分时预约比例与分时容量上限 \(b_{r,t,\tau}\) 作为显式决策变量：在核心区物理供给受限时，优先通过改变需求到达时间，而不是假设可以无限扩充停车位或窗口。无本地预约行为数据的片区只比较预约覆盖率情景，不能评价真实预约政策效果。

因此，问题三的完整目标改为：

\[
\min\ \sum_{r,t,\tau}\left[
C(x_{r,t,\tau})
\alpha_w g_{r,t,\tau}^{w}
\alpha_b\Psi_b(g_{r,t,\tau}^{b})
\alpha_p\bigl(g_{r,t,\tau}^{p}+d_rq_{r,t,\tau}^{\mathrm{transfer}}\bigr)
\alpha_c\Psi_c\left(\frac{D_{r,t,\tau}}{Q_{r,t,\tau}^{\mathrm{visit}}}\right)
\right].
\]

成本、候车和步行体验优先用 VOT 货币化后相加；安全超载与承载约束保持硬约束或以预先声明的大罚项处理。熵权法仅作为敏感性对照，不替代决策者价值权衡。旅游季节性指标（年内、月内、周内熵冗余或峰谷比）作为结果解释和高压阈值的辅助证据，不替代客流预测模型。

Q3 的可比基线定义为“按历史峰值需求乘安全系数的全年静态配置”，与动态方案使用同一批需求情景和同一成本、缺口、容量压力指标比较。求解后报告各片区、各资源约束的松弛量/活跃状态，并逐项放松约束以识别停车、接驳、人力或承载面积中的真实瓶颈；不在缺少基线时宣称任何“提升百分比”。

### 6.2 明确不纳入主模型的文献做法

- 不采用逐游客快速通行收费、实时预约再定价：缺少游客支付意愿、实时订单和价格响应数据；
- 不采用完整 \(G_t/GI/s_t+GI\) 排队系统的参数校准：缺少小时到达流、服务时长、弃队率和实际等待时间；
- 不采用逐停车场、逐车位的在线整数分配：缺少停车场实时容量、占用率、车辆 OD 与动态请求；
- 不把排队形态系数直接迁移到秦皇岛：缺少本地步道宽度、行走速度和队形观测，只保留为承载安全折减的敏感性方向。
- 不以规划停车配建指标反推实际停车位：该指标的分母是建筑面积，而项目缺少完整、同口径建筑面积；
- 不将外地 U 型拥挤--产出曲线的拐点数值迁移至秦皇岛，只保留“拥挤损失非线性上升”的目标函数结构。

## 6.3 问题三（重构后有效方案）：相对服务档位与触发规则优化

### 6.4 问题三绝对配置扩展（论文主方案）

在相对服务档位用于缺失台账时的稳健触发之外，进一步以问题二的片区日游客规模分位数构建“停车—接驳—三班制人员”的绝对规划配置模型。泊位需求由自驾比例、同行人数、峰时到达比例和停留时长换算；接驳车辆同时满足乘客需求和最大发车间隔；早/中/晚班满足入口团组、停车车辆与接驳驾驶三类服务负荷。

山海关古城五个具名停车场的 2,225 位为已核验节点硬约束，山海关五一临时 1,000 位为应急情景；北戴河 13,000 位仅作为 2017 历史参考情景；海港—阿那亚没有公开核心区总泊位，故容量明确标为情景参数。输出的车辆和人员均为“在给定参数下的推荐规划值”，不称为实测运营台账。实现、参数和论文口径见 `论文写作/问题三_绝对资源配置参数与结果说明.md`。

问题三不再试图由稀缺的局部停车位、单条线路班距反推出三区的绝对车辆、人员或车位总量。令问题二给出的锚点约束游客规模估计为 \(\hat V_{r,t}^{(q)}\)，以片区自身历史中位水平标准化：

\[
R_{r,t}^{(q)}=\frac{\hat V_{r,t}^{(q)}}{\operatorname{median}_{u}\hat V_{r,u}^{(0.5)}}.
\]

按 \(R\) 的片区内分位数划分低压、常规、高压、极端高压需求状态，并把人员、窗口、停车疏导和接驳组织定义为 \(0/1/2\) 三档相对配置强度。优化目标是最小化档位投入与分别标准化后的服务风险：

\[
\min\sum_{r,t}\left[c^\top x_{r,t}+\lambda_p P^{park}_{r,t}+\lambda_b P^{bus}_{r,t}+\lambda_w P^{entry}_{r,t}+\lambda_c P^{crowd}_{r,t}\right].
\]

其中每一类 \(P^j\) 只在本资源内部按需求或服务目标归一化；停车车辆、接驳乘客、入园人次和在园人数不直接相加。缺少本地运营台账的片区只输出相对增配比例、档位与触发阈值。

已核实的局部锚点只用于其适用范围：北戴河暑期旅游公交 2 路 15 分钟班距作为接驳基准服务水平；北戴河新区渔田停车场 1,272 车位、山海关高速服务区 791 车位作为外围停车/换乘缓冲约束，均不得替代核心片区总容量。

北戴河暑期临时方案按预测分位触发：达到片区暑期 75% 分位时维持 15 分钟基准班距并加密停车引导；达到 90% 分位时启用外围换乘、窗口与安保加密档；达到 90% 分位且为周末或法定假日时启用应急协调班组、停车饱和预警与错峰预约。该方案输出组织强度和触发条件，而非未经数据支持的精确增配车辆或人数。

## 7. 问题四：情景扰动、鲁棒优化与触发规则

对大型活动、连续阴雨和暑期高温晴天等情景改变外生变量与服务效率：

\[
\mathbf X_t^{(s)}=\mathbf X_t+\Delta\mathbf X_t^{(s)}.
\]

重新生成旅游压力后验样本，并采用：

\[
\min\ C+\lambda\operatorname{CVaR}_{0.9}(L).
\]

输出可执行的触发规则，例如：

- 高压状态概率超过阈值时，加密接驳和增开窗口；
- 预测压力达到历史 90% 分位且停车紧张时，启用外围换乘；
- 连续降雨时，降低部分到访服务供给但保留交通疏导和安全冗余。

敏感性分析围绕 \(\kappa\)、资源效率、活动冲击强度、降雨服务折减率和 VOT 进行，并报告“决策翻转区间”：参数在何范围内推荐档位不变、越过何阈值后方案切换。Q4 还须执行方向性检查：在相同情景集合下，鲁棒 CVaR 方案的成本不应低于 Q3 的确定性最优成本；若出现反向结果，检查约束或 CVaR 实现。

## 8. 论文结构

1. 数据清洗、来源分层与统计口径；
2. 潜在旅游压力生成模型与影响机制识别；
3. 全市年度/月度及区域日级预测与验证；
4. 预测驱动的资源配置强度优化；
5. 活动、降雨扰动下的鲁棒性检验；
6. 管理建议、模型优缺点与数据局限。
